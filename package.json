{
  "name": "cypressWeb",
  "version": "1.0.0",
  "devDependencies": {
    "cypress": "*"
  },
  "scripts": {
    "test": "npx cypress run --spec 'cypress/e2e/LoginTest.cy.js' --browser chrome --headed",
    "testConfig": "npx cypress run --spec 'cypress/e2e/LoginTestConfig.cy.js' --browser chrome --headed",
    "testPom": "npx cypress run --spec 'cypress/e2e/LoginTestPom.cy.js' --browser chrome --headed"
  }
}
